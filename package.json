{
  "name": "ntask-web",
  "version": "1.0.0",
  "description": "NTask web client application",  
  "author": "Hoang Tran",
  "scripts": {
    "start": "npm run build & npm run server",
    "build": "browserify src -t babelify -o src/public/js/app.js",
    "server": "http-server src/public -p 3001"
  },
  "dependencies": {
    "babel-preset-es2015": "^6.5.0",
    "babelify": "^7.2.0",
    "browser-request": "^0.3.3",
    "browserify": "^13.0.0",
    "http-server": "^0.9.0",
    "tiny-emitter": "^1.0.2"
  }
}
